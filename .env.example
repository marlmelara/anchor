# Anchor local development. Ports differ from the defaults so Anchor can run
# alongside another project's Postgres and Redis on the same machine.
ANCHOR_DATABASE_URL=postgres://anchor:anchor@localhost:5433/anchor?sslmode=disable
ANCHOR_REDIS_URL=redis://localhost:6380/0
